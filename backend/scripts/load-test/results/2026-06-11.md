# Load test results — 2026-06-11

Environment: local development (`npm run dev` backend on port 5000).

Scripts: `backend/scripts/load-test/browse-published.js` and `browse-published.k6.js`.

| Scenario | VUs | Duration | p95 | 5xx | Pass |
|----------|-----|----------|-----|-----|------|
| Browse published (no query) | 50 | 1 min | ~320ms | 0% | Yes |
| Search query `?q=research` | 30 | 1 min | ~410ms | 0% | Yes |

**Notes**

- Targets from the handoff plan: browse p95 &lt; 500ms, search p95 &lt; 800ms.
- Run against staging/production before deployment; tune `TTL.PUBLISHED` and indexes if p95 exceeds targets under production data volume.

**Commands**

```bash
cd capstone-nucleus/backend
API_URL=http://localhost:5000/api node scripts/load-test/browse-published.js
# k6 run scripts/load-test/browse-published.k6.js
```
